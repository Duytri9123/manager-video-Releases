# ── Web framework ──
flask>=3.0.0,<4.0.0
flask-socketio>=5.3.0,<6.0.0
simple-websocket>=1.0.0
werkzeug>=3.0.0,<4.0.0

# ── HTTP / async ──
httpx>=0.27.0
aiohttp>=3.9.0
aiofiles>=23.2.1
requests>=2.31.0

# ── Storage / utilities ──
aiosqlite>=0.20.0
pyyaml>=6.0.1
python-dateutil>=2.8.2
rich>=13.7.0

# ── Google APIs (YouTube) ──
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.100.0
google-genai>=1.0.0

# ── Audio / video ──
pydub>=0.25.1
imageio-ffmpeg>=0.4.9
numpy>=1.26,<3.0
Pillow>=10.0.0
cairosvg>=2.7.0  # optional: SVG rendering (needs Cairo DLL on Windows)

# ── Speech / transcription / TTS ──
faster-whisper>=1.0.0
edge-tts>=6.1.0
gtts>=2.5.0
# elevenlabs SDK optional (install manually if needed: pip install elevenlabs)
# Note: Windows users may need to enable Long Path support first.
# Our implementation uses aiohttp directly and does NOT require the SDK.

# ── Browser automation (TikTok upload, Douyin fallback) ──
playwright>=1.42.0

# ── Tunneling ──
pyngrok>=7.2.6

# ── Douyin signing ──
gmssl>=3.2.2

# ── Proxy / SOCKS support ──
PySocks>=1.7.1

# ── Optional: improve ZIP/Comic upload UX (already in werkzeug for now) ──
